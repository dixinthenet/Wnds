para hacer: 1. Corrección de Idioma (Localización) en Auditpol:
El script actual intenta parsear la salida de texto plano de 'auditpol.exe' buscando palabras en inglés como "Subcategory" o "Policy". Esto hace que falle en sistemas operativos en español u otros idiomas. Modifica esa sección para que la verificación de la política "ProcessCreation Cmdline (4688)" se realice consultando directamente el Registro de Windows (en 'HKLM:\Software\Microsoft\Windows\CurrentVersion\Policies\System\Audit') o utilizando los GUIDs nativos de auditpol, evitando depender del idioma de la consola.

2. Verificación Real de WDAC (Windows Defender Application Control):
Actualmente, el script solo comprueba si existen archivos '.cip' en el disco para asumir que WDAC está activo. Cambia o complementa esta lógica utilizando el cmdlet 'Get-CimInstance -Namespace root\Microsoft\Windows\CI -ClassName MSFT_CIPolicySetting' para verificar el estado real de ejecución (si las políticas están realmente en modo 'Enforce', 'Audit' o inactivas).

3. Añadir Control de Antivirus y Tamper Protection:
Agrega un nuevo bloque de comprobación dentro del script que verifique el estado de Windows Defender. Debe comprobar si la protección en tiempo real está activa ('RealTimeProtectionEnabled') y si la protección contra alteraciones ('Tamper Protection') está encendida, utilizando 'Get-MpComputerStatus'.

4. Soporte para Salida Estructurada (Objetos/JSON):
El script actual solo imprime texto plano con colores en la pantalla (diseñado para humanos). Modifica las funciones internas que generan los hallazgos para que, además de mostrar el texto en consola, acumulen los datos en un objeto personalizado de PowerShell ([PSCustomObject]). El script final debe permitir que el usuario pueda canalizar el resultado (por ejemplo: '.\i.ps1 | ConvertTo-Json') para exportar los datos limpiamente.

Por favor, devuélveme el código completo del script de PowerShell con estas mejoras integradas.
